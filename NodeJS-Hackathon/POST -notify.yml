info:
  name: POST /notify
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/notify
  body:
    type: json
    data: |
      {
        "email": "test@example.com",
        "productId": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
