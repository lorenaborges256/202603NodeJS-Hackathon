info:
  name: PATCH /products/:id
  type: http
  seq: 2

http:
  method: PATCH
  url: http://localhost:3000/products/1
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
